{
  "name": "svelte-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "scrape": "node ../../scripts/scrape_puzzle.mjs public/puzzles",
    "predev": "npm run scrape || echo \"Could not fetch the NYT puzzle; starting dev server without it.\"",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "@tsconfig/svelte": "^5.0.4",
    "color-convert": "^3.1.2",
    "svelte": "^5.39.6",
    "svelte-check": "^4.0.0",
    "typescript": "^5.7.0",
    "vite": "^7.1.7"
  }
}
